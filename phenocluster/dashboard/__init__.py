"""
PhenoCluster Streamlit Dashboard
================================

Optional interactive dashboard for exploring pipeline results, with a
particular focus on the v0.3.0 generalizability outputs (temporal and
multi-site validation cohorts).

Streamlit is **not** a runtime dependency of the core package. Install
the optional extras to enable the dashboard::

    pip install phenocluster[dashboard]

The CLI command :code:`phenocluster dashboard <results_dir>` launches the
Streamlit app pointed at a previously-saved pipeline output directory.
"""

__all__ = []
